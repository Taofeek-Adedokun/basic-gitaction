name: Output information
on:
  push:
    branches:
      - main 
jobs:
  info:
    runs-on: ubuntu-latest
    steps:
       - name: Output Github context
         run: echo "${{ toJson(github.event) }}"